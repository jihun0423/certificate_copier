# 📋 스펙 복사기 (Spec Clipboard)

취업 준비 중 채용 사이트에 스펙을 반복 입력하는 번거로움을 해결하기 위해 만든 **데스크탑 클립보드 앱**입니다.  
자격증, 학력, 경력, 프로젝트, 교육사항 등 자기소개서 작성에 필요한 정보를 한 곳에 모아두고, 버튼 하나로 복사할 수 있습니다.

<br>

## ✨ 주요 기능

- **항목별 복사** — 자격증 번호, 취득일, 발급기관 등 각 필드를 개별 복사
- **전체 복사** — 프로젝트·경력 등 긴 내용을 한 번에 복사
- **항상 위 고정** — 채용 사이트와 나란히 띄워두고 사용 (📌 ON/OFF 토글)
- **탭 구성** — 자격증 / 기본정보 / 학력 / 경력·봉사 / 프로젝트 / 교육사항
- **추가·수정·삭제** — 앱 내에서 직접 데이터 관리 가능
- **데이터 영속성** — JSON 파일로 저장되어 재실행해도 유지
- **창 위치·크기 기억** — 마지막 상태 그대로 복원

<br>

## 🖥️ 실행 환경

- Python 3.x
- 추가 라이브러리 설치 불필요 (표준 라이브러리 `tkinter`, `json`, `os` 사용)

<br>

## 🚀 설치 및 실행

### 1. 저장소 클론

```bash
git clone https://github.com/{jihun0423}/{certificate_copier}.git
cd {certificate_copier}
```

### 2. 실행

```bash
python spec_app.py
```

> Python이 설치되어 있으면 별도 패키지 설치 없이 바로 실행됩니다.

<br>

## 📁 파일 구조

```
📦 repo
 ┣ 📄 spec_app.py          # 메인 앱
 ┣ 📄 certs_data.json      # 자격증 데이터 (최초 실행 시 자동 생성)
 ┣ 📄 specs_data.json      # 학력·경력·프로젝트 데이터 (최초 실행 시 자동 생성)
 ┣ 📄 coursework_data.json # 교육사항 데이터 (최초 실행 시 자동 생성)
 ┣ 📄 .gitignore
 └ 📄 README.md
```

> JSON 파일은 앱 실행 후 데이터를 입력하면 자동으로 생성됩니다.  
> 개인정보가 담기므로 `.gitignore`에 등록하여 깃허브에 올라가지 않도록 주의하세요.

<br>

## 📌 사용 방법

1. `python spec_app.py` 실행
2. 상단 탭에서 원하는 카테고리 선택
3. **+ 추가** 버튼으로 본인의 스펙 입력
4. 카드를 클릭하면 세부 항목이 펼쳐짐
5. 각 항목 옆 **복사** 버튼 클릭 → `Ctrl+V`로 붙여넣기

<br>

## ⚠️ 주의사항

JSON 파일에는 개인정보(자격증 번호, 학력, 경력 등)가 저장됩니다.  
아래 `.gitignore` 설정을 반드시 추가하세요.

```
certs_data.json
specs_data.json
coursework_data.json
```

<br>

## 🛠️ 개발 환경

| 항목 | 내용 |
|------|------|
| Language | Python 3 |
| GUI | tkinter (표준 라이브러리) |
| 데이터 저장 | JSON |
| OS | Windows (Mac/Linux 호환 가능) |
